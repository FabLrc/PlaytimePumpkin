[package]
name = "playtime_pumpkin"
version = "0.1.3"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
pumpkin-plugin-api = { path = "vendor/pumpkin-plugin-api" }
tracing = "0.1"

[profile.release]
lto = true
